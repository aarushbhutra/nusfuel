# Legacy AWS Terraform

This directory is retained only as historical reference. NUSFuel now runs on Railway with its Go API and Railway PostgreSQL.

Do not run `terraform init`, `terraform plan`, or `terraform apply` here. Railway deployment configuration lives in the repository root and the Railway project.
