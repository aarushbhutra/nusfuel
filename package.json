{
  "name": "nusfuel",
  "private": true,
  "scripts": {
    "mobile:dev": "npm --prefix mobile run dev",
    "mobile:web": "npm --prefix mobile run web",
    "mobile:test": "npm --prefix mobile test",
    "mobile:lint": "npm --prefix mobile run lint",
    "smoke:release": "node tests/smoke/release.mjs",
    "smoke:test": "node --test tests/smoke/release.test.mjs"
  }
}
