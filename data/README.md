# NUSFuel seed data

Techno Edge seed files live in `techno-edge/`, one JSON file per stall.

Each file keeps the raw NUS PDF-derived dish rows plus stall-level source metadata. The backend seed loader converts those rows into the shared menu item contract and validates required nutrition, source confidence, and allergen status.
